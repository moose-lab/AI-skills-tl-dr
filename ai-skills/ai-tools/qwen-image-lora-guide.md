# Qwen-Image LoRA 训练与推理 — 入门教程

> **适合谁**：没有 GPU、没有编程经验，但想训练自己的 AI 图片风格
> **你需要**：10-20 张图片 + 一张信用卡（花费约 1-3 美元）
> **预计时间**：准备图片 15 分钟 + 训练等待 10 分钟 + 生图即时

---

## 1. 先搞清楚两个概念

**Qwen-Image** 是阿里（通义）开源的 AI 图片生成模型，能根据文字描述生成图片。它特别擅长生成中英文文字、真实人像和细腻的画面质感。

**LoRA**（Low-Rank Adaptation）是一种"微调"技术——你不需要重新训练整个巨大的模型，只需要给它"加一层薄薄的记忆"，让它学会你想要的风格、人物或物体。

**打个比方：**
- Qwen-Image 是一个万能画家
- LoRA 是你给画家看的一组参考照片，让他记住"以后画这个人/风格就按照这些照片来"
- 训练 = 让画家记住参考照片
- 推理 = 让画家根据记忆画新图

---

## 2. 你不需要自己搭环境

传统的 LoRA 训练需要一张昂贵的显卡（至少 24GB 显存），还要装一堆软件。但现在有云服务帮你搞定这一切——你只需要：

1. 准备好图片
2. 上传到平台
3. 点击训练
4. 用训练好的模型生图

下面介绍三个平台，**从最适合新手的开始**。

---

## 3. 准备训练图片（所有平台通用）

这是最关键的一步，直接决定训练效果。

### 需要多少张？

**10-20 张**，不需要很多，但质量要高。

### 图片要求

- **清晰**：不要模糊、不要低分辨率，建议至少 1024×1024
- **多样**：不同角度、不同光线、不同背景
- **一致**：所有图片展示的是同一个主题（同一个人 / 同一种风格 / 同一个物体）
- **格式**：JPG、PNG、WEBP 都行

### 打包方式

把所有图片放进一个文件夹，然后**压缩成 ZIP 文件**：
- Mac：选中所有图片 → 右键 → "压缩"
- Windows：选中所有图片 → 右键 → "发送到" → "压缩(zipped)文件夹"

### 关于"触发词"（Trigger Word）

训练时你需要设定一个"触发词"——以后生图时，在描述里加上这个词，模型就知道要用你训练的风格。

**规则：用一个不常见的词，不要用日常用语。**

| 好的触发词 | 不好的触发词 |
|-----------|-------------|
| `mys7yle` | `style`（太常见） |
| `p3r5on` | `person`（太常见） |
| `xk9cat` | `cat`（太常见） |
| `brandlogoABC` | `logo`（太常见） |

---

## 4. 平台 A：Replicate（最适合新手）

Replicate 有完整的网页操作界面，全程不需要写代码。

### 第 1 步：注册账号

1. 打开 [replicate.com](https://replicate.com)
2. 用 GitHub 账号登录（如果没有 GitHub，先注册一个）
3. 绑定付款方式

### 第 2 步：进入训练页面

1. 打开 [replicate.com/qwen/qwen-image-lora-trainer/train](https://replicate.com/qwen/qwen-image-lora-trainer/train)
2. 你会看到一个表单页面

### 第 3 步：填写训练参数

| 项目 | 填什么 | 说明 |
|------|--------|------|
| **input_images** | 上传你的 ZIP 文件 | 点击上传按钮 |
| **trigger_word** | 填你想好的触发词，比如 `mys7yle` | 以后生图要用到 |
| **steps** | 填 `1000`（默认值就行） | 训练步数，新手用默认 |
| **Destination** | 选择你的账号下的模型名 | 训练结果保存在哪里 |

其他参数保持默认。

### 第 4 步：点击 Train 开始训练

点击页面底部的 **Train** 按钮。训练通常需要 5-15 分钟。页面会实时显示进度。

> **成功的标志**：页面显示 "Succeeded"，并且出现训练完成的模型链接。

### 第 5 步：用训练好的模型生图

1. 训练完成后，点击生成的模型链接
2. 在 **prompt** 框里输入描述，**记得包含你的触发词**
3. 点击 **Run**
4. 等几秒钟，图片就出来了

**示例 prompt：**
```
A portrait of mys7yle sitting in a coffee shop, warm lighting, photorealistic
```

### Replicate 的优点

- 全程网页操作，不用写代码
- 有社区画廊，可以浏览别人训练好的风格直接用
- 训练和推理在同一个平台

---

## 5. 平台 B：fal.ai（有网页 + API）

fal.ai 提供了网页操作界面和 API 两种方式。

### 注册

1. 打开 [fal.ai](https://fal.ai)
2. 注册账号，绑定付款方式
3. 在账户设置中获取 API Key（如果需要用 API）

### 网页操作训练

1. 打开 [fal.ai/models/fal-ai/qwen-image-trainer](https://fal.ai/models/fal-ai/qwen-image-trainer)
2. 上传你的 ZIP 文件（拖拽上传或点击上传）
3. 设置参数：
   - **steps**：`1000`（默认）
   - **trigger_phrase**：你的触发词
   - **learning_rate**：`0.0005`（默认，不用改）
4. 点击 **Run**
5. 等待训练完成，下载生成的 LoRA 文件

### 价格

- **训练**：$0.002 / 步，1000 步 = **$2**
- **推理**（生图）：按次计费

### 网页推理（生图）

训练完成后，在 fal.ai 的 Qwen Image 推理页面加载你的 LoRA 文件，输入 prompt 即可生图。

### API 方式（可选，适合想进一步学习的人）

如果你想用代码调用，fal.ai 提供了 Python SDK：

```python
import fal_client

# 训练
result = fal_client.subscribe("fal-ai/qwen-image-trainer", arguments={
    "images_data_url": "https://你的图片ZIP地址.zip",
    "trigger_phrase": "mys7yle",
    "steps": 1000
})
print("训练完成！LoRA 文件地址：", result["lora_weights"]["url"])

# 推理（生图）
image = fal_client.subscribe("fal-ai/qwen-image", arguments={
    "prompt": "A portrait of mys7yle in a garden, soft light",
    "loras": [{"path": result["lora_weights"]["url"], "scale": 1.0}]
})
print("图片地址：", image["images"][0]["url"])
```

---

## 6. 平台 C：WaveSpeedAI（API 为主，速度最快）

WaveSpeedAI 主打速度，号称训练速度是普通方式的 10 倍。

### 注册

1. 打开 [wavespeed.ai](https://wavespeed.ai)
2. 注册账号
3. 在 Dashboard 获取 API Key

### 网页操作

1. 打开 [wavespeed.ai/models/wavespeed-ai/qwen-image-2512-lora-trainer](https://wavespeed.ai/models/wavespeed-ai/qwen-image-2512-lora-trainer)
2. 上传 ZIP 文件
3. 设置触发词和参数
4. 开始训练

### 价格

- **训练**：$1 / 1000 步（最便宜）
- **推理**：$0.025 / 张，6-10 秒出图

### API 方式

**训练：**

```bash
curl -X POST "https://api.wavespeed.ai/api/v3/wavespeed-ai/qwen-image-2512-lora-trainer" \
  -H "Authorization: Bearer 你的API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "data": "https://你的图片ZIP地址.zip",
    "trigger_word": "mys7yle",
    "steps": 1000,
    "learning_rate": 0.0004,
    "lora_rank": 16
  }'
```

**查看训练结果：**

```bash
curl "https://api.wavespeed.ai/api/v3/predictions/你的任务ID/result" \
  -H "Authorization: Bearer 你的API_KEY"
```

**推理（生图）：**

```bash
curl -X POST "https://api.wavespeed.ai/api/v3/wavespeed-ai/qwen-image/text-to-image-2512-lora" \
  -H "Authorization: Bearer 你的API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "A portrait of mys7yle in a garden, cinematic lighting",
    "loras": [{"path": "训练完成后返回的LoRA文件地址", "scale": 1.0}]
  }'
```

### WaveSpeedAI 的优点

- 训练价格最低（$1/1000步）
- 速度最快
- 训练和推理在同一平台，API 统一

---

## 7. 三个平台对比

| | Replicate | fal.ai | WaveSpeedAI |
|---|-----------|--------|-------------|
| **最适合** | 纯新手，不想写代码 | 想要网页+API两用 | 追求性价比和速度 |
| **操作方式** | 全网页 | 网页 + API | 网页 + API |
| **训练费用（1000步）** | ~$2-3 | $2 | **$1** |
| **推理费用** | 按秒计费 | 按次计费 | **$0.025/张** |
| **训练速度** | 5-15 分钟 | 5-15 分钟 | 几分钟 |
| **LoRA 导出** | 平台内使用 | `.safetensors` | `.safetensors` |
| **中文支持** | 一般 | 一般 | 好（页面有中文） |

**新手推荐路径**：先用 **Replicate** 的网页跑通一遍全流程，理解了之后再用 **WaveSpeedAI** 省钱。

---

## 8. 常见问题

**训练完了，生成的图片不像我的参考图？**
→ 检查是否在 prompt 里写了触发词。触发词写错一个字都不行。
→ 图片数量太少（少于 10 张）或图片质量太差（模糊、低分辨率）。
→ 训练步数太少，试试 1500-2000 步。

**触发词到底应该怎么取？**
→ 用一个无意义的自造词，比如 `ohw1k3r`、`sty99x`。不要用 `beautiful`、`woman`、`style` 这类模型已经认识的词。

**训练一次要花多少钱？**
→ 默认 1000 步：Replicate ~$2-3，fal.ai $2，WaveSpeedAI $1。第一次建议用默认参数。

**图片一定要配文字说明吗？**
→ 不一定。如果你不提供文字说明（caption），平台会自动用触发词作为所有图片的标注。但如果你能给每张图配一句简单描述（比如"一个人站在海边"），效果会更好。

**怎么给图片写文字说明？**
→ 每张图片配一个同名的 .txt 文件。例如 `photo1.jpg` 配 `photo1.txt`，txt 里写一句描述。把图片和 txt 一起打包成 ZIP。

**训练好的 LoRA 能下载吗？能在别的地方用吗？**
→ fal.ai 和 WaveSpeedAI 导出的是标准 `.safetensors` 文件，可以下载后在 ComfyUI 等本地工具中使用。Replicate 的主要在平台内使用。

**LoRA 的 scale（强度）怎么调？**
→ 默认 `1.0` 即可。如果生成的图片"过度像训练图"（失真），降低到 `0.7-0.8`。如果"不够像"，提高到 `1.2-1.5`。最大一般不超过 `2.0`。

---

## 9. 完整操作流程回顾

```
准备 10-20 张高清图片
        │
        ▼
   压缩成 ZIP 文件
        │
        ▼
  选一个平台注册账号
  （推荐新手用 Replicate）
        │
        ▼
   上传 ZIP + 设置触发词
        │
        ▼
   点击"训练" → 等 5-15 分钟
        │
        ▼
   训练完成，得到 LoRA 模型
        │
        ▼
   在推理页面输入 prompt（包含触发词）
        │
        ▼
   生成你专属风格的图片！
```

---

## 10. 信息来源

- Qwen-Image 官方仓库：[github.com/QwenLM/Qwen-Image](https://github.com/QwenLM/Qwen-Image)
- Replicate LoRA Trainer：[replicate.com/qwen/qwen-image-lora-trainer](https://replicate.com/qwen/qwen-image-lora-trainer)
- Replicate 社区 Fine-tunes：[replicate.com/collections/qwen-image-fine-tunes](https://replicate.com/collections/qwen-image-fine-tunes)
- fal.ai Qwen Image Trainer：[fal.ai/models/fal-ai/qwen-image-trainer](https://fal.ai/models/fal-ai/qwen-image-trainer)
- WaveSpeedAI LoRA Trainer：[wavespeed.ai/models/wavespeed-ai/qwen-image-2512-lora-trainer](https://wavespeed.ai/models/wavespeed-ai/qwen-image-2512-lora-trainer)
- WaveSpeedAI 推理 API：[wavespeed.ai/models/wavespeed-ai/qwen-image/text-to-image-2512-lora](https://wavespeed.ai/models/wavespeed-ai/qwen-image/text-to-image-2512-lora)

---

## 11. 质量反馈（预留）

- 清晰度评分：⭐️⭐️⭐️⭐️⭐️
- 可执行性评分：⭐️⭐️⭐️⭐️⭐️
- 实用反馈：👍 / 👎
